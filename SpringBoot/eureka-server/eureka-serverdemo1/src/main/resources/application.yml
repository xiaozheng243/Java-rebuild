server:
  port: 7001
eureka:
  instance:
    hostname: eureka1-server.com
  client:
    register-with-eureka: false
    fetch-registry: false
    # eureka-server-u-r-l-context: http://${eureka.instance.hostname}:${server.port}/eureka/
    service-url:
      defaultZone: http://eureka2-server.com:7002/eureka/,http://eureka3-server.com:7003/eureka/